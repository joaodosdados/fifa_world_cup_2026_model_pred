# Notebooks

O treinamento de produção foi migrado para:

```bash
python scripts/train_models.py
```

Assim, não é necessário instalar Jupyter ou configurar kernels para gerar os
modelos do dashboard.

O notebook `model_explanation.ipynb` permanece apenas como material didático
opcional sobre ELO e Poisson. Ele não participa do pipeline de produção.
